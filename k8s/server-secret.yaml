apiVersion: v1
kind: Secret
metadata:
  name: server-secrets
  namespace: usage-dashboard
type: Opaque
stringData:
  api-key: "REPLACE_WITH_SECURE_KEY"
  claude-token: ""
  claude-refresh-token: ""
  claude-client-id: ""
  zai-api-key: ""
  ollama-cookie: ""
  umans-api-key: ""
  # Reserved for the planned automated Ollama login (mint/refresh the session
  # cookie from credentials instead of pasting it by hand). Not yet consumed by
  # the server code — do NOT treat these as dead config or remove them.
  ollama-email: ""
  ollama-password: ""
